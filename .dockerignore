# Dependencies
node_modules
.pnpm-store

# Next.js build output
.next
out

# Environment secrets — NEVER include in image
.env
.env.local
.env.production
.env.*.local

# Version control
.git
.gitignore

# Editor / OS artifacts
.vscode
.idea
*.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Testing
coverage
.nyc_output

# Docs / misc
docs
README.md
*.md

# CI
.github

# Perf (2026-09-06) — 7 trang demo thiết kế (~3000 dòng, ~200KB source) chỉ
# dùng để tham chiếu vibe khi dev; không đưa vào image production để build
# nhanh hơn và standalone nhẹ hơn. Vẫn build ở CI job `quality` để không
# hỏng khi refactor component chung.
src/app/vibe-demo
