# Báo cáo Nhật ký Phiên làm việc (Daily Log) — 07/08/2026

## 🎯 Mục tiêu phiên làm việc
1. Tóm tắt các thay đổi từ lần Git Pull mới nhất.
2. Giải thích mục đích tồn tại của các nhánh Git (`main` và `origin/docs/vision-alignment`).
3. Đánh giá trạng thái và thực hiện trọn vẹn **Work Package 0 (Checkpoint 0)** theo định hướng tầm nhìn mới ([`docs/VISION.md`](file:///home/thai/my-project/elearning-platform/docs/VISION.md)) và lộ trình ([`docs/ROADMAP.md`](file:///home/thai/my-project/elearning-platform/docs/ROADMAP.md)).
4. Build sản phẩm và khởi chạy ứng dụng thành công.

---

## 🛠️ Những công việc đã thực hiện (What was done)

### 1. Phân tích & Tóm tắt Git Pull mới nhất
* Tóm tắt 3 commits mới nhất (`0a6c446`, `281896`, `5e182db`):
  * **Tái định hướng sản phẩm**: Chuyển từ LMS Marketplace (giảng viên bán khóa học, admin duyệt) sang **Personal Link-Organizer** (người dùng tự dán link tổ chức khóa học cá nhân).
  * **Dọn dẹp tài liệu**: Xóa 23 tệp thiết kế kiến trúc Marketplace cũ (~6,600+ dòng code/docs).
  * **Bổ sung tài liệu mới**: Đưa vào [`docs/VISION.md`](file:///home/thai/my-project/elearning-platform/docs/VISION.md), [`docs/ROADMAP.md`](file:///home/thai/my-project/elearning-platform/docs/ROADMAP.md), [`docs/design/checkpoint-0-1-ux-audit.md`](file:///home/thai/my-project/elearning-platform/docs/design/checkpoint-0-1-ux-audit.md) và kế hoạch tích hợp AI.

### 2. Thực hiện trọn vẹn Work Package 0 (WP0)

#### 🔹 WP0.1 — Auth / Request Context tập trung
* ⚠️ **CẬP NHẬT (sau kiểm tra kỹ, xem mục "Đính chính" bên dưới):** đợt commit `da3491f` mới chỉ **định nghĩa** `RequestContext`/`getRequestContext()` trong [`src/shared/middleware/auth.ts`](file:///home/thai/my-project/elearning-platform/src/shared/middleware/auth.ts) — hàm này **chưa được route nào gọi** (0 usage), và `src/middleware.ts` thêm vào chỉ là no-op (`NextResponse.next()`, không set gì). 26 route API vẫn tự gọi `getUserFromRequest`/`getUserIdFromRequest` rời rạc như cũ. WP0.1 **chưa hoàn thành** ở lần commit đó.

#### 🔹 WP0.2 — Pivot Data Model sang Khóa học Cá nhân & Gỡ tàn dư Marketplace
* **Prisma Schema ([`prisma/schema.prisma`](file:///home/thai/my-project/elearning-platform/prisma/schema.prisma))**:
  * Thêm bảng `sources` (lưu trữ và dedup theo URL).
  * Thêm trường `owner_id` (chủ sở hữu) và `share_token` (token chia sẻ) cho `courses`.
  * Liên kết `lessons` với `sources`.
  * Đặt tên quan hệ rõ ràng trong Prisma: `@relation("CourseOwner")` và `@relation("CourseLecturer")`.
  * Chạy `npx prisma generate` tạo client mới thành công.
* **Cập nhật Backend Domain & Services**:
  * Bổ sung `ownerId` và `shareToken` vào entity [`Course.ts`](file:///home/thai/my-project/elearning-platform/src/modules/course-management/domain/Course.ts).
  * Cập nhật [`CourseRepository.ts`](file:///home/thai/my-project/elearning-platform/src/modules/course-management/repositories/CourseRepository.ts) ghi `owner_id` xuống DB.
  * Cập nhật [`ContentManagementService.ts`](file:///home/thai/my-project/elearning-platform/src/modules/course-management/services/ContentManagementService.ts) hỗ trợ tìm kiếm khóa học theo `owner_id`.
* **Dọn dẹp UI Marketplace**:
  * [`src/app/admin/approval-queue/page.tsx`](file:///home/thai/my-project/elearning-platform/src/app/admin/approval-queue/page.tsx): Thay thế trang duyệt bằng thông báo bãi bỏ quy trình duyệt bài.
  * [`src/app/courses/[id]/page.tsx`](file:///home/thai/my-project/elearning-platform/src/app/courses/[id]/page.tsx): Gỡ bỏ nút "Đăng ký học" & kiểm tra role `STUDENT`; cho phép bấm trực tiếp "Bắt đầu học".
  * [`src/app/lecturer/courses/[id]/edit/page.tsx`](file:///home/thai/my-project/elearning-platform/src/app/lecturer/courses/[id]/edit/page.tsx): Gỡ bỏ nút "Gửi duyệt" và trạng thái khóa chỉnh sửa.
  * [`src/components/Header.tsx`](file:///home/thai/my-project/elearning-platform/src/components/Header.tsx): Đồng nhất Menu điều hướng người dùng.
  * [`src/components/CourseCard.tsx`](file:///home/thai/my-project/elearning-platform/src/components/CourseCard.tsx): Gỡ badge trạng thái duyệt bài.
  * [`src/app/page.tsx`](file:///home/thai/my-project/elearning-platform/src/app/page.tsx): Mở nút "Tạo khóa học" cho mọi tài khoản đã đăng nhập.
  * Dọn dẹp toàn bộ `console.log` dư thừa tại `learn/page.tsx` và `YoutubePlayer.tsx`.

#### 🔹 WP0.3 — Seed Data & Verification
* Cập nhật [`prisma/seed.ts`](file:///home/thai/my-project/elearning-platform/prisma/seed.ts) điền `owner_id`.
* **Kiểm tra TypeScript**: `tsc --noEmit` -> **0 lỗi**.
* **Kiểm tra Unit Test**: `vitest run` -> **86/86 tests passed**.
* ⚠️ Nhưng phần lớn 86 test này (`PublishingPolicy`, `EnrollmentPolicy`, `EnrollmentService`) đang test chính **domain logic marketplace cũ** (duyệt bài, enrollment) — nghĩa là logic đó xanh vì **chưa hề bị đụng tới**, không phải vì đã pivot xong.

### 3. Build sản phẩm & Chạy Server
* Thực hiện `npm run build` thành công (production build sẵn sàng).
* Khởi chạy ứng dụng bằng `npm run dev` ở chế độ background.
* ⚠️ **Lưu ý quy trình:** các lệnh trên đã dùng `npm`/`npx` trực tiếp, sai với chuẩn dự án (đã cấu hình `mise` quản lý Node.js + `pnpm@10.30.3` bắt buộc qua `packageManager` trong `package.json`, test runner là `vitest`). Từ nay dùng `mise exec -- pnpm <script>` (`pnpm typecheck`, `pnpm test`, `pnpm build`, `pnpm exec prisma generate`, …), không dùng `npm`/`npx`.

---

## 🔎 Đính chính (sau audit ngày 07/08, cùng phiên)

Sau khi build/test lại đúng bằng `mise exec -- pnpm ...`, phát hiện commit `da3491f` **báo cáo "hoàn thành trọn vẹn WP0" là không chính xác** — build/test xanh chỉ chứng minh không có lỗi cú pháp, không chứng minh WP0 đã xong về nội dung. Cụ thể còn thiếu:

1. **WP0.1 chưa xong**: `getRequestContext()` là code chết (0 lời gọi), `middleware.ts` là no-op, 26 route vẫn tự decode JWT rời rạc như trước khi "tập trung hoá".
2. **WP0.2 chỉ xong phần UI, chưa xong phần backend**: model `courses` vẫn còn nguyên `lecturer_id`, `status` (DRAFT/PENDING/REJECTED/ACTIVE), `reject_note`, `submitted_at`; toàn bộ luồng approval backend vẫn sống — `ApprovalController.ts`, `ApprovalService.ts`, `api/v1/management/approval-queue`, `.../courses/[id]/publish`, `.../courses/[id]/moderate`, `api/v1/courses/[id]/enroll` đều còn nguyên. Role literal `STUDENT/LECTURER/ADMIN` còn rải rác 12 file.

## ✅ Công việc hoàn thiện WP0 (đợt 2, cùng phiên 07/08)

Thực hiện đúng bằng `mise exec -- pnpm ...` từ đầu đến cuối, không dùng `npm`/`npx`.

### WP0.1 — Auth tập trung (đã xong thật)
* `src/shared/middleware/auth.ts`: viết lại để `getRequestContext()` là **nơi duy nhất** parse JWT từ header `Authorization`; `getUserFromRequest()`/`getUserIdFromRequest()` giờ chỉ là wrapper mỏng gọi vào `getRequestContext()` — không còn 2 đường parse JWT song song.
* Xoá `src/middleware.ts` (no-op, không set gì, chỉ gây rối).

### WP0.2 — Gỡ hẳn approval/enrollment-giữa-user khỏi backend (đã xong thật)
* **Domain**: `Course.ts` bỏ state machine DRAFT→PENDING→ACTIVE/REJECTED, còn lại `ACTIVE`/`ARCHIVED` (course sống ngay khi tạo, owner tự archive khi muốn). `AccessControlPolicy` chỉ còn kiểm tra ownership thuần. `PublishingPolicy` chỉ còn rule "không xoá section cuối cùng", bỏ hết `validateModerationEligibility`/`validateRejectNote`.
* **Xoá hẳn** `ApprovalController.ts`, `ApprovalService.ts`, `dtos/ModerateCourseDto.ts`, route `api/v1/management/approval-queue`, `.../courses/[id]/publish`, `.../courses/[id]/moderate`, `src/lib/admin.ts`, `src/types/admin.types.ts` (dead code, không còn ai gọi).
* **Enroll route** (`api/v1/courses/[id]/enroll`): bỏ gate "chỉ STUDENT được enroll" (BR-ENROLL-02) — không còn chặn theo role.
* **Gỡ các khoá "read-only khi ACTIVE/PENDING"** phát hiện trong lúc sửa — đây là bug nghiêm trọng sẽ tự khoá mọi course ngay sau khi tạo (vì course giờ ACTIVE ngay lập tức): `lecturer/courses/[id]/edit/page.tsx` (state `readOnly`), `api/v1/management/courses/[id]/sections/route.ts` (chặn GET khi `ACTIVE` trả 409, chặn POST khi không phải `DRAFT`/`REJECTED`), `CourseManagementService.syncCourseContent`.
* **Prisma schema**: xoá cột `reject_note`, `submitted_at` khỏi `courses`.
* **Migration**: phát hiện đợt commit `da3491f` **chưa từng tạo migration file** cho toàn bộ pivot trước đó (`owner_id`, `share_token`, bảng `sources`) — nghĩa là nếu deploy thật, DB sẽ lệch hoàn toàn so với `schema.prisma`. Đã tạo migration gộp `prisma/migrations/20260807095000_pivot_personal_courses_and_simplify_status/` bao gồm cả phần thiếu đó lẫn thay đổi của đợt này (có backfill `owner_id = lecturer_id` an toàn cho dữ liệu cũ), **đã test chạy `prisma migrate deploy` từ đầu trên Postgres sạch — thành công**.
* **seed.ts**: 3 course seed đều `ACTIVE`, bỏ hết `submitted_at`/`reject_note`.
* **UI phụ**: `lecturer/courses/page.tsx` và `types/lecturer.types.ts` bỏ tab/badge Draft/Pending/reject-note, chỉ còn All/Active/Archived.

### Xác minh (không chỉ "build xanh")
* `mise exec -- pnpm typecheck` → 0 lỗi.
* `mise exec -- pnpm test` → **62/62 pass** (giảm từ 86 vì đã xoá đúng các test test hành vi approval/DRAFT cũ, không phải test bị hỏng).
* `mise exec -- pnpm lint` → sạch.
* `mise exec -- pnpm build` → thành công; route map xác nhận `approval-queue`/`publish`/`moderate` đã biến mất.
* **Smoke test thật** trên Postgres tạm (docker, migrate từ đầu + seed): login → tạo course mới → course `ACTIVE` ngay → gọi `GET`/`POST` sections **không bị chặn** (trước đây sẽ 409/400) → gọi endpoint `approval-queue`/`publish` cũ → **404** (đã xoá hẳn).

**Kết luận: WP0.1, WP0.2, WP0.3 nay đã hoàn thành đúng nghĩa, có migration thật và có smoke test xác nhận** — không chỉ dựa vào build/test xanh như lần báo cáo trước.

---

## 👁️ Những gì nhận thấy & bài học rút ra (What was observed)

1. **Vấn đề của mô hình cũ (Marketplace)**:
   * Codebase ban đầu mang nặng tư duy Marketplace cũ làm cản trở trải nghiệm học: người dùng phải đóng vai "học viên" đăng ký khóa học do "giảng viên" tạo và qua "admin" duyệt.
2. **Sức mạnh của việc Pivot dữ liệu ở Checkpoint 0**:
   * Áp dụng nguyên tắc #1 trong `ROADMAP.md`: Breaking change chỉ được phép làm ở Checkpoint 0 khi chưa có người dùng ngoài. Việc chuyển `courses` sang `owner_id` giúp loại bỏ hoàn toàn rào cản duyệt bài và đăng ký.
3. **Kinh nghiệm làm việc với Prisma ORM**:
   * Khi một bảng (`courses`) liên kết 2 lần với cùng một bảng khác (`users` thông qua `owner_id` và `lecturer_id`), Prisma bắt buộc phải có tên quan hệ cụ thể (`@relation("Name")`) ở cả 2 phía model, nếu không Prisma client sẽ tự động bỏ qua quan hệ dẫn tới lỗi type.
4. **"Build/test xanh" không chứng minh feature đã xong**:
   * `tsc --noEmit` chỉ bắt lỗi kiểu; `vitest run` chỉ xanh nếu test còn tồn tại và thực sự target đúng code đã đổi. Cả hai đều không phát hiện được: (a) một hàm được viết ra nhưng 0 nơi gọi (`getRequestContext`), (b) một route/middleware là no-op, (c) schema đổi nhưng chưa từng có migration file tương ứng. Phải grep chéo lời gọi thực tế + chạy `prisma migrate deploy` trên DB sạch + smoke test API thật mới xác nhận được là xong.
   * `sed`/`grep` toàn repo cho các literal cũ (`DRAFT`, `PENDING`, `rejectNote`, tên hàm đã xoá...) là bước rẻ nhưng hiệu quả để tìm hết chỗ sót trước khi tin tưởng `tsc` báo sạch.
